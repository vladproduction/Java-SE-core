package com.vladproduction.tasks;

public class IntegerComparison {
    public static void main(String[] args) {
        /*Integer i = new Integer(5); // Create a new Integer object with value 5
        Integer b = i;               // b now references the same Integer object as i
        i = 5;                       // i now points to an Integer object representing the value 5 (autoboxing occurs)

        // At this point:
        // b points to the original Integer object created with `new Integer(5)`
        // i points to a new Integer object representing the value 5 (autoboxed)

        System.out.println(i == b); // Comparing the references (not the values)*/
    }
}