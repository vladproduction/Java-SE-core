package com.vladproduction.interfaces;

/**
 * Defining an Interface: An interface in Java is a reference type, similar to a class,
 * that can contain constants, method signatures, default methods, static methods, and nested types.
 * Interfaces are abstract by nature, meaning they can't be instantiated directly.
 * */
public interface Animal {
    void eat();
    void sleep();
}